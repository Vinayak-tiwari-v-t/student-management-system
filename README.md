# Student Management System

A Java command-line application to manage student records.

## Features
- Add student records (ID, Name, GPA)
- Display all student records
- Search student records by ID

## How to Run
1. Open a terminal/command prompt.
2. Compile the program:
   ```bash
   javac Main.java
4. run the program:
 ```bash
   java Main
   
